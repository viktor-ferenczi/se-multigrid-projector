<?xml version="1.0"?>
<PluginData xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:type="GitHubPlugin">

    <!-- The ID of your plugin, never change this -->
    <Id>viktor-ferenczi/se-multigrid-projector</Id>

    <!-- GitHub repository ID -->
    <RepoId>viktor-ferenczi/se-multigrid-projector</RepoId>

    <!-- The name of your plugin that will appear in the plugin list -->
    <FriendlyName>Multigrid Projector</FriendlyName>

    <!-- The author name that you want to appear in the plugin list -->
    <Author>Viktor</Author>

    <!-- Optional tooltip for the plugin in the plugin list -->
    <Tooltip>Allows loading blueprints of multiple sub-grids into projectors and building them.</Tooltip>

    <!-- Optional plugin description. If omitted, this will be the same as the Tooltip. 1000 characters max. -->
    <Description>This plugin allows to load blueprints of multiple sub-grids into projectors and welding the blocks. Head/base blocks are added automatically as needed to connect subgrids.

For support join the SE Mods Discord:
https://discord.gg/PYPFPGf3Ca

Please consider supporting my work on Patreon:
https://www.patreon.com/semods

One time support via PayPal:
https://www.paypal.com/paypalme/vferenczi/

Thank you!
    </Description>

    <!-- Only source code in the Directory entries listed in SourceDirectories is compiled. Exclude any test projects or similar. -->
    <SourceDirectories>
        <Directory>ClientPlugin</Directory>
        <Directory>Shared</Directory>
        <Directory>MultigridProjectorApi</Directory>
    </SourceDirectories>

    <!-- The current plugin version's commit hash -->
    <Commit>TODO Git commit hash</Commit>

</PluginData>
